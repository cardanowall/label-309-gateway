# Locally installed dependencies. This tool installs standalone, outside the
# pnpm workspace, so its node_modules is generated by `npm install` here.
node_modules/

# Per-case comparison reports are derived output, not source. Regenerate with
# `node generate-oracle.mjs`.
out/
